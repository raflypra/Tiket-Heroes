# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tiket Heroes' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tiket Heroes
  pod 'SwiftMessages'
  pod 'SwiftyJSON'
  pod 'Alamofire', '~> 4.8'
  pod 'AlamofireImage', '~> 3.5'
  pod 'JGProgressHUD'
  pod 'LoadingPlaceholderView'

  target 'Tiket HeroesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Tiket HeroesUITests' do
    # Pods for testing
  end

end
